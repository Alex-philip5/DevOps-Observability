global:
  resolve_timeout: 5m

route:
  receiver: 'slack-notifications'

receivers:
- name: 'slack-notifications'
  slack_configs:
    - api_url: "<webhook>"
      channel: 'monitoring-alert'
      send_resolved: true
